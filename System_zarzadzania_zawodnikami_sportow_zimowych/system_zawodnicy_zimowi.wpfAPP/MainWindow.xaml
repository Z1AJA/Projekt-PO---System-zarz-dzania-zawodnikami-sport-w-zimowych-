<Window x:Class="system_zawodnicy_zimowi.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="System Zawodników - Manager (z Punktacją)" Height="700" Width="1100"
        WindowStartupLocation="CenterScreen">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*" />
                <ColumnDefinition Width="1.5*" />
            </Grid.ColumnDefinitions>

            <GroupBox Header="Baza Zawodników" Grid.Column="0" Margin="0,0,10,0">
                <DataGrid x:Name="ListaDatagrid" 
                          AutoGenerateColumns="False" 
                          IsReadOnly="True"
                          SelectionMode="Single"
                          SelectionChanged="ListaDatagrid_SelectionChanged"
                          BorderThickness="0">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Imię" Binding="{Binding Imie}" Width="*"/>
                        <DataGridTextColumn Header="Nazwisko" Binding="{Binding Nazwisko}" Width="*"/>
                        <DataGridTextColumn Header="Wiek" Binding="{Binding Wiek}" Width="50"/>
                        <DataGridTextColumn Header="Ranga" Binding="{Binding Ranga}" Width="80"/>
                        <DataGridTextColumn Header="Dyscyplina" Binding="{Binding Dyscyplina}" Width="120"/>
                        <DataGridTextColumn Header="Klub" Binding="{Binding KlubNazwa}" Width="100"/>
                        <DataGridTextColumn Header="Punkty" Binding="{Binding Punkty}" Width="60" FontWeight="Bold"/>
                    </DataGrid.Columns>
                </DataGrid>
            </GroupBox>

            <ScrollViewer VerticalScrollBarVisibility="Auto" Grid.ColumnSpan="2" Margin="611,0,0,0">
                <StackPanel>

                    <GroupBox Header="Rejestracja Zawodnika" Padding="10" Margin="0,0,0,15" BorderBrush="#FF444444">
                        <StackPanel>
                            <TextBlock Text="Wybierz typ:" FontWeight="SemiBold"/>
                            <ComboBox x:Name="CmbTyp" Margin="0,0,0,10" Padding="5">
                                <ComboBoxItem Content="Narciarz Alpejski" IsSelected="True"/>
                                <ComboBoxItem Content="Snowboardzista"/>
                            </ComboBox>

                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0" Margin="0,0,5,0">
                                    <TextBlock Text="Imię:"/>
                                    <TextBox x:Name="TxtImie" Margin="0,0,0,10"/>
                                </StackPanel>
                                <StackPanel Grid.Column="1" Margin="5,0,0,0">
                                    <TextBlock Text="Nazwisko:"/>
                                    <TextBox x:Name="TxtNazwisko" Margin="0,0,0,10"/>
                                </StackPanel>
                            </Grid>

                            <TextBlock Text="Wiek:"/>
                            <TextBox x:Name="TxtWiek" Margin="0,0,0,10" Width="50" HorizontalAlignment="Left"/>

                            <Button Content="DODAJ DO BAZY" Click="BtnDodaj_Click" Height="30" Background="#FF3399FF" Foreground="White" FontWeight="Bold"/>
                        </StackPanel>
                    </GroupBox>

                    <GroupBox Header="Dodaj Wynik Zawodów" Padding="10" Margin="0,0,0,15" BorderBrush="Green">
                        <StackPanel>
                            <TextBlock Text="Dla wybranego zawodnika:" Foreground="Gray" Margin="0,0,0,5"/>
                            <TextBlock x:Name="TxtWybranyDoWyniku" Text="-- wybierz z listy --" FontWeight="Bold" Margin="0,0,0,10"/>

                            <TextBlock Text="Nazwa Zawodów:"/>
                            <TextBox x:Name="TxtNazwaZawodow" Margin="0,0,0,5"/>

                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0" Margin="0,0,5,0">
                                    <TextBlock Text="Miejsce (1-300):"/>
                                    <TextBox x:Name="TxtMiejsce" Margin="0,0,0,5"/>
                                </StackPanel>
                                <StackPanel Grid.Column="1" Margin="5,0,0,0">
                                    <TextBlock Text="Trudność (1-5):"/>
                                    <TextBox x:Name="TxtTrudnosc" Margin="0,0,0,5"/>
                                </StackPanel>
                            </Grid>

                            <TextBlock Text="Punkty Bazowe:"/>
                            <TextBox x:Name="TxtPunktyBazowe" Margin="0,0,0,10"/>

                            <TextBlock Text="Data zawodów:"/>
                            <DatePicker x:Name="DateDataZawodow" Margin="0,0,0,10" SelectedDate="{x:Static system:DateTime.Today}" xmlns:system="clr-namespace:System;assembly=mscorlib"/>

                            <Button Content="DODAJ WYNIK I PRZELICZ" Click="BtnDodajWynik_Click" Height="40" Background="#FF4CAF50" Foreground="White" FontWeight="Bold"/>
                        </StackPanel>
                    </GroupBox>

                    <GroupBox Header="Transfery Klubowe" Padding="10">
                        <StackPanel>
                            <TextBlock Text="Nazwa Klubu:"/>
                            <TextBox x:Name="TxtNazwaKlubu" Margin="0,0,0,10"/>

                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="10"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Button Grid.Column="0" Content="Przypisz" Click="BtnPrzypiszKlub_Click" Height="25"/>
                                <Button Grid.Column="2" Content="Wypisz" Click="BtnWypiszKlub_Click" Height="25"/>
                            </Grid>
                        </StackPanel>
                    </GroupBox>
                </StackPanel>
            </ScrollViewer>
        </Grid>

        <Separator Grid.Row="1" Margin="0,10"/>

        <Border Grid.Row="2" Background="#F0F0F0" Padding="15" CornerRadius="5">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" VerticalAlignment="Center" Margin="0,0,20,0">
                    <TextBlock Text="ZAWODNIK" FontSize="10" Foreground="Gray"/>
                    <TextBlock x:Name="TxtImieStopka" Text="Wybierz zawodnika" FontSize="16" FontWeight="Bold"/>
                    <TextBlock x:Name="TxtWiekStopka" Text="-- lat" FontSize="12" Foreground="DarkSlateGray"/>
                </StackPanel>

                <Grid Grid.Column="1" VerticalAlignment="Center">
                    <ProgressBar x:Name="PasekPostepu" Height="25" Minimum="0" Maximum="20000" Value="0" Foreground="#FF3399FF"/>
                    <TextBlock x:Name="TxtPunktyPasek" Text="0 pkt" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="Black" FontWeight="SemiBold"/>
                </Grid>

                <StackPanel Grid.Column="2" VerticalAlignment="Center" Margin="20,0,0,0" MinWidth="100">
                    <TextBlock Text="STATUS" FontSize="10" Foreground="Gray"/>
                    <TextBlock x:Name="TxtRangaStopka" Text="---" FontWeight="Bold" Foreground="#FF5500" FontSize="14"/>
                    <TextBlock x:Name="TxtDyscyplinaStopka" Text="---" FontWeight="SemiBold"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>